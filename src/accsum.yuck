Usage: axp [QUOTES] < ACCOUNTS

Analyse the market during periods of exposure.

Output is number of regime switches, total time spent within a regime,
and the total amount invested within a regime.

  -e, --edge            Treat direction changes as regime switch,
                        default: levels.
  -g, --gross           Do not take commissions into account.
  -v, --verbose         Print explanations.
